-- Zig. Binary: download zls from https://github.com/zigtools/zls/releases
-- (match your Zig version) and put it on your PATH.
return {
  settings = {
    zls = {
      enable_inlay_hints = true,
    },
  },
}
